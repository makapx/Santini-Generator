version: '3.9'

services:

  production-service:
    build: .
    ports:
      - "80:80"

  development-service:
    image: node:alpine
    volumes:
      - .:/santini-gen
    ports:
      - "80:80"

    command: sh -c "cd /santini-gen && npm install && npm start"  

  
